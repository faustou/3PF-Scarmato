import { NameCompletePipe } from './name-complete.pipe';

describe('NameCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new NameCompletePipe();
    expect(pipe).toBeTruthy();
  });
});
